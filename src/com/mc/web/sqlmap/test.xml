<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Test">
	
	<select id="test" parameterType="Map" resultType="mcmap">
		SELECT TO_CHAR(SYSDATE, 'YYYY/MM/DD HH:MM:SS') as dt FROM DUAL
	</select>
	
	<select id="carallbaro_uuid" parameterType="Map" resultType="mcmap">
		SELECT '/upload/board/' || YYYY ||'/'|| MM ||'/'|| UUID AS CARALL_UUID, '/upload/board/' || YYYY ||'/'|| MM ||'/' AS PATH FROM NSH_ATTACH WHERE TABLE_NM='NSH_CARALLBARO' 
	</select>

</mapper>